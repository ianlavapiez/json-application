# JSON Population Exercise

## Running the App

After cloning the project, first, install the dependencies from the root directory.

```bash
npm install
```

Then you can start the app by executing the following command:

```bash
npm run dev
```
